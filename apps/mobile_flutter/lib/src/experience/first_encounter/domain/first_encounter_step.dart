import 'first_encounter_phase.dart';

class FirstEncounterStep {
  const FirstEncounterStep({required this.phase, required this.duration});

  final FirstEncounterPhase phase;
  final Duration duration;
}
